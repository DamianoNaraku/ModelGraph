ngxx - context - menu;
works
    < /p>
    < ul >
     * ngFor;
"let item of items"[contextMenu] = "basicMenu"[contextMenuSubject] = "item" > Right;
Click: {
    {
        item ? .name : ;
    }
}
/li>
    < /ul>
    < context - menu >
    -template;
contextMenuItem(execute) = "showMessage('Hi, ' + $event.item.name)" >
    Say;
hi
    < /ng-template>
    < ng - template;
contextMenuItem;
divider = "true" > /ng-template>
    < ng - template;
contextMenuItem;
let - item(execute);
"showMessage($event.item.name + ' said: ' + $event.item.otherProperty)" >
    Bye, {};
{
    item ? .name : ;
}
/ng-template>
    < ng - template;
contextMenuItem;
passive = "true" >
    Input;
something: type;
"text" >
    /ng-template>
    < /context-menu>;
//# sourceMappingURL=context-menu.component.html.js.map