{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "allowSyntheticDefaultImports": true, // my add
    "experimentalDecorators": true, // my add
    "types": ["node", "jquery", "jquery-ui", "jqueryui"] // node is my add, was empty.
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
